<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Well… This Is Awkward</title>

  <!-- Tailwind CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
<nav class="fixed right-6 top-1/2 -translate-y-1/2 z-50">
  <div class="bg-black/90 border border-white/10 rounded-2xl shadow-xl
              flex flex-col gap-3 p-4">
    <%if(user){%>
      <a href="/user/<%= user._id %>"
      class="px-4 py-2 text-sm font-bold text-white bg-yellow-600
             rounded-xl hover:bg-yellow-700 transition text-center">
      Home
    </a>
      <a href="/profile/<%= user._id %>"
      class="px-4 py-2 text-sm font-bold text-white bg-green-600
             rounded-xl hover:bg-green-700 transition text-center">
      Profile
    </a>
    <a href="/posts/<%=user._id%>"
      class="px-4 py-2 text-sm font-bold text-white bg-gray-700
             rounded-xl hover:bg-gray-600 transition text-center">
      My Posts
    </a>

    <a href="/post/create/<%=user._id%>"
      class="px-4 py-2 text-sm font-bold text-white bg-purple-600
             rounded-xl hover:bg-purple-700 transition text-center">
      Create Post
    </a>
    <%}else{%><a href="/"
      class="px-4 py-2 text-sm font-bold text-white bg-indigo-600
             rounded-xl hover:bg-indigo-700 transition text-center">
      Login/Register
    </a><%}%>
    <a href="/admin"
      class="px-4 py-2 text-sm font-bold text-white bg-red-600
             rounded-xl hover:bg-red-700 transition text-center">
      Admin
    </a>
  </div>
</nav>
<main class="relative w-screen h-screen flex items-center justify-center overflow-hidden bg-[url('/images/black-glitch-effect-texture-background.jpg')] bg-cover">

  <!-- Subtle Glow -->
  <div class="absolute inset-0 bg-[radial-gradient(circle_at_top,_rgba(255,255,255,0.08),transparent_40%)]"></div>

  <!-- Error Card -->
  <section class="relative z-10 w-[90%] max-w-xl bg-white/10 backdrop-blur-xl
  border border-white/20 rounded-3xl p-10 text-center shadow-2xl">

    <!-- Icon -->
    <div class="text-7xl mb-4">🤦‍♂️</div>

    <!-- Sarcastic Title -->
    <h1 class="text-4xl font-extrabold text-white tracking-wide">
      Uhh… dude.
    </h1>

    <!-- Fixed Sarcastic Text -->
    <p class="text-gray-300 mt-4 text-lg leading-relaxed">
      Something went wrong.  
      We don’t know what.  
      It just… did.
    </p>

    <p class="text-gray-400 mt-3">
      Try again. Or don’t. That’s also an option.
    </p>

    <!-- Actions -->
    <div class="flex flex-col sm:flex-row gap-4 mt-10">
      <a href="/" class="flex-1 bg-white text-black font-bold py-3 rounded-xl hover:bg-gray-200 transition">
        Take Me Home
      </a>
      <button onclick="history.back()" 
        class="flex-1 border border-white text-white font-bold py-3 rounded-xl hover:bg-white/10 transition">
        Go Back
      </button>
    </div>

    <!-- Footer Line -->
    <p class="text-xs text-gray-500 mt-6 italic">
      This page exists because computers have emotions too.
    </p>

  </section>

</main>

</body>
</html>